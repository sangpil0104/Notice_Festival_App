
import 'package:flutter/material.dart';

class Category3Screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Category 3', style: TextStyle(fontSize: 24)),
    );
  }
}
